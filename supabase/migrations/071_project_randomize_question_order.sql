-- ============================================================
-- 071: 設問順ランダマイズの簡単トグル（統計エクスポート §3 / パターン1）
-- ブロック(ページグループ)を作らなくても、プロジェクト単位で
-- 「設問をランダム表示する」を1スイッチで有効化できるようにする。
-- 依存（前問参照・piping 等）は配信時に自動で順序保持される。
--
-- 後方互換: DEFAULT false。既存案件は従来どおりマスター順。
-- ============================================================

ALTER TABLE projects
  ADD COLUMN IF NOT EXISTS randomize_question_order boolean NOT NULL DEFAULT false;
