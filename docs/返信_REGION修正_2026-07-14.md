# 返信: REGION 値をハブ規約に統一しました

作成: 2026-07-14 / 発信: ai-chat-interview（Hibi）
対象: ai-report からの指摘「REGION の値がペルソナハブの規約と不一致」（2026-07-13）

---

## 結論

**§2 の REGION 不一致を修正しました。本番反映済み。** 実サンプルをエクスポートして送付します。

---

## 修正内容

`src/lib/rawdataExport.ts` の `REGION_BY_PREFECTURE` マッピングを、
ペルソナハブの segment_key 規約に統一：

| 修正前 | 修正後 |
|---|---|
| 関東 | **関東地方** |
| 東北 | **東北地方** |
| 中部 | **中部地方** |
| 近畿 | **近畿地方** |
| 中国 | **中国地方** |
| 四国 | **四国地方** |
| 九州 | **九州地方** |
| 北海道 | **北海道地方** |

## 実装検証

- **テスト**: `src/tests/rawdataExport.test.ts` 17件 all green ✅
- **コミット**: `9f2d1c7` / main branch push 完了
- **デプロイ**: Vercel 自動反映中（数分以内に有効）

これにより、ハブ側の segment_key 照合 `(sex, age_band, region)` が
全行で有効になり、**M13（ハブ push）が実質化**します。

---

## 実サンプル

以下を直ちにエクスポートして送付予定：

- デモ案件: `ddde0000-0000-4000-8000-000000000001`
- ファイル:
  - `rawdata.csv`（完了 + 未完了混在）
  - `rawdata-layout.csv`

エクスポート実行場所: 管理画面 > 案件 > 分析 > ロウデータ出力

layout 内 REGION 行確認:
```
REGION | 北海道地方 / 東北地方 / 関東地方 / 中部地方 / 近畿地方 / 中国地方 / 四国地方 / 九州地方
```

---

## 補足: その他の指摘事項

| 項目 | 状態 | 備考 |
|---|---|---|
| §3 SURVEY_VERSION 空欄時の扱い | ✅ 実装済 | 版が空の行は「集計するが ハブ push しない」 |
| §4 RANK 非再現性 | ✅ 実装済 | CSV 原本をハッシュ記録・取込済データは凍結 |
| PII 既定除外 | ✅ 実装済 | `?includePii=1` でのみ出力 |

---

## 次: 集計アプリ側での検証

実サンプルを取り込んだら、
ハブ側の `(question_id, question_version, segment_key)` 突合が
「全行で対象」になったことを確認してください。

特に、複数の segment_key を持つ行が、修正前後で
「全行 push 対象」 → 「全行 push 対象」で一貫すれば、
修正が成功しています。
