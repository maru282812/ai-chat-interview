# 返信: 実サンプル検収の残不備修正＋未完了行入りサンプル再送付

作成: 2026-07-17 / 発信: ai-chat-interview（Hibi）/ 宛先: 集計アプリ（ai-report）
対象申し送り: 「実サンプル検収結果と残る不備の修正依頼」（2026-07-17）

## 0. 結論

**§1（layout 規約違反4列）と §2（未完了行）は修正・対応済み。** 再エクスポートした実サンプル1式を本返信と同時に渡す。

- 送付物: `rawdata_2026-07-17.csv`（**60行** = COMP 50 / PARTIAL 6 / ABANDON 4）+ `rawdata-layout_2026-07-17.csv`（168行）
- §3（SURVEY_VERSION）は凍結運用の開始時期を §3 に回答
- §4（layout のハブ突合キー列）は**既に出力済み**（今回サンプルで確認可能）

## 1. 【対応済み】選択肢定義の規約違反4列 → 「1選択肢=1行（code+label 非空）」に修正

修正箇所は `src/lib/rawdataExport.ts` の `buildRawdataLayoutRows`（layout 生成の単一実装。今後の全エクスポートに適用される）。

| 列 | 修正後 |
|---|---|
| `AGE_BAND` | 9行に分解: `10`=10代（18〜19歳） / `20`=20代 / … / `80`=80代 / `90`=90代以上 |
| `PRE` | 47行に分解: JIS X 0401 の `1`=北海道 … `47`=沖縄県（ラベル省略ではなく全行出力） |
| `CHI` | 2行に分解: `1`=あり / `2`=なし |
| `IS_TEST` | 2行に分解: `1`=テスト回答 / `0`=本番回答（note に「既定出力ではテスト回答（1）の行を除外」を明記） |

- 年代コード表は `AGE_BAND_CODES` として定数化した。**今後の新規列も「1選択肢=1行」規約に乗せる**（自動テストで PRE=47行 / AGE_BAND=9行 / CHI=2行 / IS_TEST=2行・code+label 非空を固定済み）。
- 既存列の順序・名前・意味は変更していない（凍結契約維持。REGION / JOB / BUS / TIME_SEC / RANK は前回検収OKの形のまま）。

## 2. 【対応済み】未完了行入りサンプルの再エクスポート

今回のサンプルはデモ案件に **PARTIAL 6行 / ABANDON 4行** を追加した60行構成。

- 未到達設問は規約どおり**センチネル `94`**（テキスト列は空欄）で出力されている
- 未完了行は `END` 空欄・`TIME_SEC` は開始→最終回答時刻の実測値入り
- 全60行で属性列（SEX / AGE / AGE_BAND / PRE / REGION / JOB / BUS / MAR / INC / CHI）に実値が入るようにした（AGE_BAND の各コード・8地方が出現する分布。生年月日未登録者も混在＝AGE/AGE_BAND 空欄の検証が可能）

これで M2（クリーニング裁定）の実データ検証ができるはず。

## 3. 【回答】SURVEY_VERSION の凍結運用の開始時期

- 凍結（スナップショット確定）の**機能は実装済み・運用は未開始**。現時点の回答に版が付かないのはこのため（契約どおり空欄）。
- **開始時期: 実案件の初回配信前（営業開始前）から必須運用にする。** 具体的には「配信ボタンを押す前に必ず凍結する」を案件公開チェックリストに入れ、凍結済みでない案件の実配信を行わない運用とする。
- 凍結開始以降の回答から `SURVEY_VERSION` に版数が入り、自動的にハブ push（M13）対象になる想定で問題ない。
- デモ案件はスナップショット未確定のため、今回サンプルも全行空欄のまま（仕様どおり）。

## 4. 【回答】layout の question_id / question_version / trait_key 列

**v2 実装時点で既に出力している。** 今回の再エクスポートで確認できる:

- layout ヘッダー末尾3列 = `question_id` / `question_version` / `trait_key`
- 設問列（43行）に `question_id` が入っている。メタ・属性列は空欄
- `question_version` は行の SURVEY_VERSION と同じ理由（凍結未開始）で当面空欄、`trait_key` は変数の metric_code 未設定のため空欄（ハブ辞書登録後に入る）

前回サンプルに無かったのは、v2 拡張のデプロイ前に生成したサンプルだったためと思われる。暫定規約（question_id=layout の question_code 突合）からの正式切替はハブ側の都合に合わせられる。

## 5. 受け入れ条件との対応

- [x] `AGE_BAND` / `CHI` / `IS_TEST` が「1選択肢=1行（code+label 非空）」で layout に出力される
- [x] `PRE` が47行で出力される（ラベル省略ではなく全行）
- [x] 再エクスポートに未完了行（PARTIAL 6 / ABANDON 4）がセンチネル `94` 付きで含まれる
- [x] SURVEY_VERSION の凍結運用の開始時期を共有（§3: 実案件の初回配信前から必須運用）
- [x] 実サンプル1式（rawdata.csv + rawdata-layout.csv）を渡す

## 6. 再現手順（Hibi 側メモ）

```
node scripts/seedAiReportDemoProject.mjs seed      # デモ案件を60人構成で再seed（teardown で撤去可）
npx tsx scripts/exportAiReportSample.ts            # exports/rawdata/ に rawdata + layout を実エクスポート
```

エクスポートは本番 admin 画面と同じ `statExportService.rawdataCsv / rawdataLayoutCsv` を使用（送付物と本番出力が乖離しない）。
