# 統計エクスポート改修 実装状況（2026-06-29）

改修指示「アンケート・インタビュー結果を外部統計ソフトで扱えるようにする」（全23項目）の実装状況。
方針: **後方互換（§12）を守り、既存CSV出力・回答フローは一切変更しない追加実装**。今回はDB変更なしで
完結する「エクスポート層」と「送付前バリデーション層」を純関数中心で実装。DB＋ランタイム改修が必要な
項目は次フェーズへ整理した。

## 実装済み（このブランチ: feat/statistical-export-engine）

### コードブック導出層 — [src/lib/codebook.ts](../src/lib/codebook.ts)（§7 / §20）
- 設問→統計変数定義（variable_name / data_type / measure_type / allowed_values /
  multi_select_encoding / free_text_policy / aggregation_policy / scale範囲）をマイグレーション無しで導出。
- 既定値は question_type / question_config から導出。管理者上書きは `question_config.meta.cleaning` から読む。
- 欠損値センチネル `DEFAULT_MISSING_CODES`（無回答=`.` / 不明=99 / 拒否=98 / 対象外=97 /
  スクリーニング落ち=96 / 分岐非表示=95 / 未到達=94）。変数名は統計ソフト向けに安全化（`__free_comment__`→`free_comment`）。

### エクスポート生成エンジン — [src/lib/statExport.ts](../src/lib/statExport.ts)（§2 §8–§11 §15 §19 §20 §21）
- `buildWideRows` 1回答者1行・**列は送付時マスター順（既定 sort_order, §12）**・多重選択は **one-hot 展開（§15）**
  `<var>_<option_code>=0/1` ＋ `<var>_other_text`・欠損はセンチネル・テスト回答は既定除外（§17）。
- AI深掘り統合値（§9）: ai_probe_enabled 設問に `_final_answer_text / _cleaned_value / _extracted_json /
  _probe_triggered / _probe_count / _probe_reason / _probe_questions_json / _probe_answers_json`。
  **比較単位は常に元の question_code（§8）。深掘りは回答者数に加算しない。**
- `buildLongRows` 1回答1行・primary / ai_probe を別行・`parent_answer_id` 紐づけ・実表示順(display_position)・
  §10 深掘りバイアス用メタ（probe_reason / probe_question_text / probe_model / probe_prompt_version /
  probe_completion_score / probe_confidence）。
- `buildCodebookRows` / `buildSnapshotJson`（読み取り側スナップショット）/ `buildRandomizationLogRows`。
- `toCsvRfc4180` UTF-8 BOM + RFC4180 + CRLF（§21）。
- §19: 出力主キーは擬似匿名 `respondent_key`（= respondent.id）。line_user_id 等は出力しない。

### サービス・エンドポイント — [src/services/statExportService.ts](../src/services/statExportService.ts)
GET（管理画面・要Basic認証）:
- `/admin/projects/:projectId/exports/stat/respondents-wide.csv`（`?includeTest=1` で全件）
- `/admin/projects/:projectId/exports/stat/answers-long.csv`
- `/admin/projects/:projectId/exports/stat/codebook.csv`
- `/admin/projects/:projectId/exports/stat/questionnaire-snapshot.json`
- `/admin/projects/:projectId/exports/stat/randomization-log.csv`

### 送付前バリデーション — [src/lib/surveyValidation.ts](../src/lib/surveyValidation.ts)（§4 §5 §6 §13）
- 依存エッジを branch_rule / visibility_conditions / display_tags_parsed(piping/answerInsertion/disable) /
  `meta.dependencies` から導出。
- 検出: question_code重複 / 出力変数名重複 / 依存先・分岐先・表示条件参照先の欠落 / 循環依存 /
  **依存順違反（A依存のBがB→A順＝マスター順逆転、§13）** / 空・重複 option value / 選択肢欠落 /
  順序依存の文言（「前の質問」「上記」等）と依存未宣言の矛盾（§5・警告）。
- GET `/admin/projects/:projectId/validate` がJSONレポート（ok / errorCount / findings / dependencies）を返す。

### テスト — [src/tests/statExport.test.ts](../src/tests/statExport.test.ts)
codebook / wide one-hot / 欠損センチネル / AI深掘り統合値 / long probe紐づけ / RFC4180+BOM /
バリデーション各種、計9件 pass。typecheck・既存テストも green。

## 追加実装（Phase A〜E・2026-06-29 同日）

DB変更を伴う後続フェーズを、後方互換（マイグレーション未適用／未設定なら従来挙動）で実装。
**マイグレーション 067〜069 は未適用**（DB反映は別途実行が必要）。コードはカラム不在でも安全に動くよう防御的に実装。

- **§17/§18 is_test・channel** — migration 067（respondents.is_test）。export は実 is_test とチャネル（project_assignments.delivery_channel）を出力。
- **§1/§14 スナップショット永続化** — migration 068（questionnaire_snapshots＋sessions.snapshot_id）。[snapshotService](../src/services/snapshotService.ts)（内容ハッシュで版を再利用、変更で version++）。POST `/admin/projects/:id/snapshot`（確定）/ GET `/admin/projects/:id/snapshots`（一覧）。export は有効スナップショットの codebook を列順の基準にする（§2/§13）。
- **§6 送付ゲート** — POST `/admin/projects/:id/snapshot` が `validateSurvey` を実行し、error があれば 400 でブロック（`?force=1` で警告のみ無視可）。GET `/admin/projects/:id/validate` は従来どおりレポートのみ。
- **§3/§22 ブロック・ランダム化** — migration 069（question_blocks／questions.block_id／sessions.randomization_seed・display_order_json）。[randomization.ts](../src/lib/randomization.ts) はシード再現可能・**依存順を壊さない**（制約付きトポロジカルソート）決定的アルゴリズム。export はセッションに記録された display_order/seed を採用（無ければマスター順）。
- **§19 同意スコープ** — wide/long/randomization 各エクスポートで `?consentedOnly=1`（任意 `&consentDocType=<type>`）。有効な同意レコードを持つ回答者のみに絞る。

テスト計13件 pass（codebook/wide/long/RFC4180/snapshot hash/randomization/validation）。typecheck・既存テストも green。

## 追加実装（ブロック＝ページグループ統合＋配信結線＋管理UI・2026-06-29）

ユーザー方針: **ブロックは既存ページグループに統合**／**配信（回答者画面の表示順）に適用**。

- **migration 069 を作り直し**: 別テーブル question_blocks は廃止。`question_page_groups` に
  `is_randomizable` / `randomize_within` / `fix_within` を追加（§12「page_groups を初期ブロックとして扱う」に準拠）。
  sessions に `randomization_seed` / `display_order_json`（+ 068 の `snapshot_id`）。
- **配信結線（§3）** — [surveyOrderingService](../src/services/surveyOrderingService.ts)。LIFFアンケート表示時
  （[liffController surveyPage](../src/controllers/liffController.ts) の main フェーズ）に、回答者ごとの表示順を
  `computeDisplayOrder`（依存順を保持）で1度だけ確定→ seed・display_order・snapshot を session に保存→以後再利用。
  反映は DTO の sort_order を表示順位で上書き（survey.ejs はクライアントで sort_order 昇順整列）。
  **ランダム化フラグ未設定なら一切変更しない（後方互換）。** 失敗しても回答継続を妨げない。
- **スナップショット凍結（§1）**: 上記の初回確定時に `snapshotService.createOrReuse` で凍結し session.snapshot_id に保存。
- **管理UI**:
  - [pageGroups.ejs](../src/views/admin/projects/pageGroups.ejs) にランダム化フラグ（ページ間/ページ内/順序固定）の編集＋状態列。
  - [analysis.ejs](../src/views/admin/projects/analysis.ejs) に「統計エクスポート」5種ダウンロード（テスト含む/同意者のみ切替）＋
    「調査票を検証」「調査票を確定（スナップショット）」ボタン（検証エラーは確定をブロックし一覧表示）。
- 依存順エッジ抽出 `extractOrderingEdges` を [surveyValidation](../src/lib/surveyValidation.ts) から公開。

typecheck・テスト（13＋既存4）green。

## 追加（導線＋全モード対応・2026-06-30）

- **入口ボタン追加**: 設問一覧 [indexDesigner.ejs](../src/views/admin/questions/indexDesigner.ejs) と設問編集 [formV3.ejs](../src/views/admin/questions/formV3.ejs) のヘッダに「🧩 ブロック/ページ」リンクを追加（従来はURL直打ちのみだった）。
- **全表示モードでブロック/ランダム化を有効化**: ページグループ画面 [pageGroups.ejs](../src/views/admin/projects/pageGroups.ejs) の「survey_page 限定」警告を、モード別の説明に変更（survey_page=視覚ページ＋ブロック、それ以外=集計/ランダム化ブロック）。
- **survey_question の並べ替え対応**: survey.ejs は survey_question モードで配列順をそのまま使う（sort_order 再整列しない）ため、[surveyOrderingService](../src/services/surveyOrderingService.ts) で sort_order 上書きに加えて**配列自体も表示順に並べ替え**るよう修正。これで survey_page / survey_question 双方で反映される。

## AIブロック自動設計（作成を楽にするUI・2026-06-30）

「ブロック数を選ぶ→AIが判定して案を生成→同画面で流れをプレビュー→文言/番号/割当を調整→保存」を実装。

- 純ロジック [lib/blockPlan.ts](../src/lib/blockPlan.ts)（テスト可・DB/AI非依存）: `buildFallbackPlan`（マスター順の等分割・決定的フォールバック）/ `parseAiPlan`（AIのJSON→プラン・未割当は最後へ寄せる）/ `previewOrders`（回答者N人分の実表示順サンプル・依存順保持・決定的）。
- [blockDesignService](../src/services/blockDesignService.ts): `suggest`（aiService.callRaw でブロック案生成・失敗時フォールバック）/ `preview` / `apply`（既存ページグループを作り直し、設問の割当と sort_order を設計どおりに更新・破壊的・明示操作のみ）。
- エンドポイント: GET `/admin/projects/:id/blocks`（設計画面）、POST `/blocks/suggest|preview|apply`。
- 画面 [blockDesigner.ejs](../src/views/admin/projects/blockDesigner.ejs): ブロック数選択＋「🤖AIでブロック分け」→ ブロック編集（タイトル/順序/設問割当/移動/ランダム化フラグ）→「回答者プレビュー（N人）」→「保存」。
- 導線: ブロック一覧（pageGroups.ejs）に「🤖 AIでブロック自動設計」ボタン、設問一覧/設問編集ヘッダに「🧩 ブロック/ページ」。
- リポジトリ追加: `questionRepository.setPageGroup`（null で割当解除可）、pageGroup `create` にランダム化フラグ。
- テスト計16件 pass（blockPlan のフォールバック/解析/プレビュー含む）。

## ランダム化の正しい階層（専門家ヒアリングで認識修正・2026-06-30）

当初「ブロック＝ページグループ」と取り違えていた。実際に必要なのは次の階層：

| 層 | 内容 | 優先 | 状態 |
|---|---|---|---|
| L1 コンセプト順ローテーション（ラテン方格） | 複数コンセプト(P/Q/R)＝同一アンケートを各回答者が全部回答、提示順を均等ローテ。3等分でなく「3倍」 | **主役** | ◯基盤実装（エンジン/モデル/サービス/管理UI）・ライブ回答フロー結線は残（下記） |
| L2 設問間（大問）順ローテーション＝**パターン1** | Q1/Q2 の順入替 | 中 | ✅実装。**プロジェクト単位の簡単トグル**（researchForm「設問をランダム表示する」/ migration 071 `projects.randomize_question_order`）＝ブロック不要で全設問を回答者ごとに入替・依存順は自動保持。詳細制御は従来のブロック方式も継続。 |
| L3 選択肢ランダマイズ | 設問内の選択肢順。「その他/特になし」はアンカー固定 | 高 | ✅実装（下記） |
| L3' 選択肢グループ・ランダマイズ | 1設問の選択肢を群に分け群順＋群内ランダム・アンカー固定 | 高 | ✅実装 |
| ＋ その他＝記述 | 「その他」に自由記述欄・あり/なし選択 | 助かる | ◯設定/フラグ実装・LIFF描画は残 |

### L3 / L3' / その他記述（実装済み・2026-06-30）
- 純エンジン [lib/optionRandomization.ts](../src/lib/optionRandomization.ts): `computeOptionOrder`（アンカーは元位置固定／群があれば群順＋群内シャッフル／seed決定的）。seedヘルパは [randomization.ts](../src/lib/randomization.ts) の `createSeededRng`/`seededShuffle`。
- 型: `QuestionConfig.option_randomization`（enabled/anchored_values/groups/randomize_groups）＋ `QuestionOption.allow_free_text`（migration不要・jsonb）。
- 配信適用 [surveyOrderingService](../src/services/surveyOrderingService.ts): ページ設定に関係なく、選択肢ランダムを常に適用（seed=`session.id:question.id`・再描画で同じ並び）。survey.ejs は選択肢を配列順で描画するため、サーバ側で options を並べ替えるだけで反映される。
- 管理UI [formV3.ejs](../src/views/admin/questions/formV3.ejs): 「選択肢ランダム化(L3)」セクション（有効/群ランダム/アンカー/群定義/自由記述をラベル名で指定）。パーサは adminController の選択肢保存部に追加。
- テスト: アンカー固定・群まとまり・決定性を検証（計19件 pass）。
- その他=記述の LIFF 描画/収集（2026-06-30 横展開済み）: list/card・single/multi の `allow_free_text` 肢に自由記述欄を描画。共通収集 `collectChoiceVal`（その他に入力したら該当肢を選択しテキストを値化）。**guarded**（allow_free_text=false の既存肢は従来どおり）。残: interview_chat 路（会話型のため対象外寄り）。要実機検証。

### L1 コンセプト・ローテーション 基盤（実装済み・2026-06-30）
- 純エンジン [lib/latinSquare.ts](../src/lib/latinSquare.ts): `latinSquareOrder`（巡回ラテン方格＝各コンセプトが各提示位置に均等）／`permutations`・`fullPermutationOrder`（全順列）／`assignConceptOrder(codes, respondentIndex, mode)`。テスト済み（ラテン方格性・全順列・off）。
- migration 070: `project_concepts`（concept_code/title/description/master_order/is_active）＋ `projects.concept_rotation_mode`(off/latin/full)＋ `answers.concept_code`＋ `sessions.concept_order_json`。
- 型 `ProjectConcept`＋Project/Answer/Session 拡張、[projectConceptRepository](../src/repositories/projectConceptRepository.ts)（テーブル未適用なら空配列＝後方互換）、[conceptService](../src/services/conceptService.ts)（CRUD＋`resolveOrder`）。
- 管理UI [concepts.ejs](../src/views/admin/projects/concepts.ejs)（ローテーション方式選択＋コンセプトCRUD）。設問一覧ヘッダに「🔁 コンセプト」。
- ライブ結線（2026-06-30 一部実装）: ①LIFFアンケート表示時に **concept_order を確定して session.concept_order_json に保存**（rotation 有効時のみ・respondentIndex=作成順）。②エクスポート: **answers_long に concept_code 列を追加**（answer.concept_code から）。
- **残（要実機検証・大）**: 回答フローで「全コンセプトを割当順に提示し、各回答へ concept_code を付与」する多コンセプト回答UI（同一設問×コンセプトの提示・回答保存への concept タグ付け・完了時の次コンセプト遷移）。これが入るまで concept_code は埋まらず、wide のコンセプト別分割も保留。answer収集/完了フローの改変は実機検証必須のため本セッションでは設計済みのデータ層までに留める。

## なお残る項目（要DB稼働確認・小）

- **マイグレーション 067〜069 の適用**と、適用後の実DB／実機（LIFF）でのエクスポート・並べ替え動作確認（私の環境では実DB検証不可）。
- **§22 エクスポート監査ログ**: 乱数シード保存は実装済み。誰が・いつ・どの版・どのフィルタで出力したかの export_jobs テーブルは未実装。
- interview_chat モードでのランダム化挙動の実機確認（会話フローは1問ずつ進むため、ブロック並べ替えの意味は限定的）。

## 次フェーズ（DB＋ランタイム改修が必要・未実装）

| 項目 | 内容 | 必要な追加 |
|---|---|---|
| §1 送付時スナップショット永続化 | 現状はエクスポート時に現定義から再構築（`snapshot_kind: export_time`）。回答後に設問が変わると過去定義が再現できない | `questionnaire_snapshots` / `question_snapshots` テーブル、送付/開始時の保存、回答→版の紐づけ |
| §14 配信ウェーブ/版管理 | スナップショットの version・wave・hash・縦断比較 | 上記テーブルに version/wave 列 |
| §3 ブロック単位ランダム化（ランタイム） | block_code・ブロック/設問内ランダム化、実表示順の記録 | blocks テーブル、配信フロー改修、`randomization_log` 永続化（現状は読み取り側でマスター順フォールバック） |
| §17 is_test / response_status / 重複・無効 | テスト回答除外・部分回答・重複判定。現状 is_test=false 固定 | answers/respondents へ `is_test` 等のフラグ列、重複検出 |
| §18 channel / パラデータ | 流入経路（LINE/店舗QR/専用URL）・設問別所要時間。現状 channel=null | project_assignments.delivery_channel 連携、設問別タイムスタンプ計測 |
| §19 同意スコープ連動 | 外部提供同意済みのみエクスポート | 既存 consent（045/046）とのフィルタ接続 |
| §22 エクスポート監査・乱数シード | 出力ログ・再現可能なシード保存 | export_jobs テーブル、randomization seed 永続化 |
| §6 一部 | label変更後の過去回答解釈（版間マッピング） | option履歴（§1スナップショットに内包） |

送付前バリデーション（§4/§5/§6）は実装済みだが、「**送付処理のゲートとして必須化**」する接続
（送付APIで errorCount>0 ならブロック）は配信フロー側に組み込みが必要。
